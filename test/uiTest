import http from 'http';
import assert from 'assert';
require('dotenv').config();

import '../lib/index.js';

describe('Cypress Hill Test', () => {
  it('page contain Hello World', done => {
      
  });
});
